import React from 'react';
import styles from './logo.module.css';

const Logo = () => {
  return (
    <a className={styles.logo} href='/'>
      Nicholas Newman
    </a>
  );
};

export default Logo;
